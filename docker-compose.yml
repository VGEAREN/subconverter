services:
  subconverter:
    build: .
    container_name: subconverter
    ports:
      - "25500:25500"
    volumes:
      - ./base/custom/pref.toml:/base/pref.toml
      - ./base/custom/ACLSSR:/base/ACL4SSR
      - ./base/custom/ACLSSR/Clash:/base/Clash
      - ./base/custom/savedconfigs:/base/custom/savedconfigs
    environment:
      # main.cpp reads MANAGED_PREFIX / API_TOKEN (not the host-facing names)
      - API_TOKEN=${API_ACCESS_TOKEN:-}
      - MANAGED_PREFIX=${MANAGED_CONFIG_PREFIX:-http://127.0.0.1:25500}
      - CUSTOM_PAGE_PASSWORD=${CUSTOM_PAGE_PASSWORD:-}
    restart: always
